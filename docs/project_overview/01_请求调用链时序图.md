# 一次完整请求的调用链时序图

## 场景：用户发送一条消息，AI 调用工具后返回最终回复

```mermaid
sequenceDiagram
    autonumber
    participant U as 用户 (UI)
    participant VM as ChatViewModel
    participant MC as MessageCoordination<br/>Delegate
    participant MP as MessageProcessing<br/>Delegate
    participant EA as EnhancedAIService
    participant AI as AIService<br/>(Provider)
    participant TEM as ToolExecution<br/>Manager
    participant Tool as 工具执行器<br/>(defaultTool/*)
    participant MR as Markdown<br/>Renderer

    U->>VM: 点击发送按钮
    VM->>MC: sendUserMessage(promptFunctionType)

    Note over MC: 检查是否有活跃对话<br/>无则创建新对话

    MC->>MC: sendMessageInternal()
    Note over MC: ① 解析角色卡 & 模型绑定<br/>② 检查 token 用量<br/>③ 必要时异步启动历史总结

    MC->>MP: sendUserMessage(attachments, chatId, ...)

    Note over MP: 构建用户消息内容
    MP->>MP: AIMessageManager<br/>.buildUserMessageContent()
    Note over MP: 拼接附件/工作区/回复引用

    MP->>MP: addMessageToChat(userMessage)
    Note over MP: 用户消息写入本地历史

    MP->>EA: EnhancedAIService<br/>.getChatInstance(chatId)
    EA-->>MP: service 实例

    MP->>EA: sendMessage(message, chatHistory, ...)

    Note over EA: 准备会话上下文
    EA->>EA: prepareConversationHistory()
    Note over EA: 注入 System Prompt：<br/>• 工具 Schema (SystemToolPrompts)<br/>• 角色设定 (CharacterCard)<br/>• 记忆查询结果<br/>• 工作区文件列表<br/>• 思考引导

    EA->>EA: getAvailableToolsForFunction()
    Note over EA: 根据权限级别选择工具集<br/>(standard/admin/root/...)

    EA->>EA: getModelParametersForFunction()

    rect rgb(230, 245, 255)
        Note over EA,Tool: 多轮工具循环 (ReAct Loop)

        EA->>AI: sendMessage(chatHistory,<br/>modelParameters, availableTools)
        Note over AI: Provider 发起流式请求<br/>(OpenAI/Claude/Gemini/本地...)

        loop 流式接收每个 chunk
            AI-->>EA: 流式文本片段
            EA-->>MR: 流式输出到渲染器
            MR-->>U: 打字机效果实时显示
        end

        Note over EA: 检测到 AI 输出中<br/>包含 <tool_call>

        EA->>EA: handleToolInvocation()

        EA->>TEM: executeInvocations(toolInvocations)
        Note over TEM: 只读工具 → 并行执行<br/>写操作工具 → 串行执行

        TEM->>Tool: execute(toolName, params)
        Note over Tool: ToolRegistration 路由<br/>→ ToolGetter 按权限选择实现<br/>→ 具体执行器运行

        Tool-->>TEM: 工具执行结果
        TEM-->>EA: allToolResults

        EA->>EA: processToolResults()
        Note over EA: 将工具结果追加到<br/>conversationHistory

        EA->>AI: sendMessage(更新后的 chatHistory)
        Note over AI: AI 根据工具结果<br/>生成下一轮回复

        loop 流式接收最终回复
            AI-->>EA: 流式文本片段
            EA-->>MR: 流式输出
            MR-->>U: 实时显示
        end

        Note over EA: AI 未再调用工具<br/>→ 循环结束
    end

    EA-->>MP: 流式完成信号

    MP->>MP: addMessageToChat(aiMessage)
    Note over MP: AI 回复写入本地历史

    MP->>MP: 更新 token 统计
    MP->>MP: 卸载 workspace hook
    MP->>MP: 更新 inputProcessingState → Idle

    MP-->>U: 消息处理完成
```

## 关键节点说明

### 1. System Prompt 注入 (`prepareConversationHistory`)

每次请求前动态生成，包含：

| 内容 | 来源 |
|------|------|
| 工具描述 (schema) | `SystemToolPrompts.kt` |
| 角色设定 | `CharacterCard` / `ActivePromptManager` |
| 记忆上下文 | 记忆库向量搜索结果 |
| 工作区文件列表 | 当前绑定的工作区目录 |
| 思考引导 | 配置开关 (`enableThinkingGuidance`) |

### 2. 工具权限路由 (`ToolGetter`)

```
用户授权级别
    ├── Standard  → defaultTool/standard/*
    ├── Admin     → defaultTool/admin/*     (Shizuku)
    ├── ADB       → defaultTool/debugger/*
    ├── A11y      → defaultTool/accessbility/*
    └── Root      → defaultTool/root/*
```

同一工具名，不同权限下路由到不同实现。例如 `shell_command`：
- Standard 模式 → 只能在沙盒内执行
- Root 模式 → 拥有完整 root shell

### 3. 工具调用循环 (ReAct Loop)

```
AI 输出 → 包含 <tool_call>? 
    ├── 是 → 执行工具 → 结果放回历史 → 再次请求 AI → 循环
    └── 否 → 输出纯文字 → 结束
```

循环终止条件：
- AI 未输出任何 `<tool_call>` 标签
- 达到最大工具调用轮次上限
- Token 超限（触发总结后自动续写）

### 4. 流式渲染管线

```
AI 流式 chunk → EnhancedAIService 
    → StreamCollector → StreamMarkdownRenderer (KMP 算法)
        → BatchNodeUpdater (批量 UI 更新) 
            → Compose SnapshotStateList (增量重组)
```
