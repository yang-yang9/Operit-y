# 屏幕装配流程图

本文档描述 Operit 应用从 Activity 入口到最终 Screen 渲染的完整装配链路。

## 架构概览

Operit 采用 **单一 Activity + 多 Composable** 架构，使用自定义导航栈（非 Jetpack Navigation），支持手机和平板两种响应式布局。

## 装配流程图

```mermaid
graph TD
    subgraph Activity层
        A[MainActivity.onCreate]
        A --> B["setContent { OperitTheme { OperitApp(...) } }"]
    end

    subgraph OperitApp 根组件
        B --> C{screenWidthDp >= 600?}
        B --> STATE["状态管理<br/>• currentScreen<br/>• selectedItem<br/>• backStack<br/>• topBarActions"]
        B --> NAV["导航函数<br/>• navigateTo()<br/>• goBack()"]
        B --> SIDE["副作用 LaunchedEffect<br/>• 快捷方式处理<br/>• 网络轮询<br/>• 公告拉取<br/>• MCP插件同步"]
    end

    subgraph 布局选择
        C -->|是 平板| D[TabletLayout]
        C -->|否 手机| E[PhoneLayout]
    end

    subgraph TabletLayout
        D --> D1["Box (fillMaxSize)"]
        D1 --> D2["Surface (侧边栏)<br/>宽度动画: 64dp ↔ 280dp"]
        D2 --> D2A{isExpanded?}
        D2A -->|是| D3[DrawerContent<br/>完整导航菜单]
        D2A -->|否| D4[CollapsedDrawerContent<br/>仅图标]
        D1 --> D5["Box (内容区)<br/>offset = sidebarWidth"]
        D5 --> F[AppContent]
    end

    subgraph PhoneLayout
        E --> E1["Box (fillMaxSize)<br/>支持拖拽手势"]
        E1 --> E2["Box (主内容)<br/>translationX 偏移动画"]
        E2 --> F
        E1 --> E3["Surface (模态抽屉)<br/>drawerWidth = 75%屏幕"]
        E3 --> E4[DrawerContent<br/>完整导航菜单]
        E1 --> E5["Box (遮罩层)<br/>点击关闭抽屉"]
    end

    subgraph AppContent 内容容器
        F --> G["Scaffold"]
        G --> G1["topBar: TopAppBar<br/>← 返回按钮 | 标题 | actions →"]
        G --> G2["content: Box"]
        G2 --> G3["背景图片 (可选)"]
        G2 --> H["currentScreen.Content(...)"]
    end

    subgraph Screen.Content 屏幕渲染
        H --> S1["Screen.AiChat → AIChatScreen"]
        H --> S2["Screen.Settings → SettingsScreen"]
        H --> S3["Screen.Packages → PackageManagerScreen"]
        H --> S4["Screen.Toolbox → ToolboxScreen"]
        H --> S5["Screen.TokenConfig (二级) → TokenConfigScreen"]
        H --> S6["... 其他 Screen"]
    end

    subgraph 覆盖层
        B --> OV["RemoteAnnouncementDialog<br/>(远程公告弹窗)"]
    end
```

## 装配链路（5 层）

| 层级 | 组件 | 职责 |
|------|------|------|
| **1. Activity** | `MainActivity` | 生命周期管理、权限检查、通过 `setContent` 启动 Compose |
| **2. OperitApp** | `OperitApp` | 全局状态管理、设备检测、布局分叉、导航逻辑 |
| **3. Layout** | `TabletLayout` / `PhoneLayout` | 侧边栏/抽屉 + 内容区的排列方式 |
| **4. AppContent** | `AppContent` | Scaffold(TopAppBar + 内容区)、屏幕缓存 |
| **5. Screen** | `Screen.Content()` | 每个 Screen 子类渲染对应的具体业务页面 |

## 关键分支点

| 分支 | 条件 | 结果 |
|------|------|------|
| **布局选择** | `screenWidthDp >= 600` | 平板: `TabletLayout` / 手机: `PhoneLayout` |
| **侧边栏形态** | `isTabletSidebarExpanded` | 展开: `DrawerContent` / 收起: `CollapsedDrawerContent` |
| **返回按钮显示** | `currentScreen.isSecondaryScreen` | 二级页面显示返回箭头 |
| **TopBar Actions** | 由子屏幕通过 `LocalTopBarActions` 注入 | AiChat/TokenConfig 有自定义按钮，其余页面清空 |

## 导航菜单结构 (DrawerContent)

```
Column (可滚动)
├── App 标题 ("Operit") + 网络状态
├── NavGroup: AI 功能
│   ├── AiChat
│   ├── AssistantConfig
│   ├── Packages
│   └── MemoryBase
├── NavGroup: 工具
│   ├── Toolbox
│   ├── ShizukuCommands
│   └── Workflow
└── NavGroup: 系统
    ├── Settings
    ├── Help
    ├── About
    └── UpdateHistory
```

## 导航状态机

```mermaid
stateDiagram-v2
    [*] --> AiChat : 启动默认

    state 一级屏幕 {
        AiChat
        Settings
        Packages
        Toolbox
        Help
        About
    }

    state 二级屏幕 {
        TokenConfig
        ModelConfig
        ThemeSettings
        MCPMarket
    }

    AiChat --> Settings : navigateTo (压 AiChat 入栈底)
    AiChat --> Packages : navigateTo (压 AiChat 入栈底)
    Settings --> AiChat : goBack (弹栈)

    AiChat --> TokenConfig : navigateTo (二级, 压栈)
    TokenConfig --> AiChat : goBack (弹栈)

    Settings --> ModelConfig : navigateTo (二级, 压栈)
    ModelConfig --> Settings : goBack (弹栈)

    note right of AiChat : AiChat 是根页面\n所有一级页面返回兜底到 AiChat
```

## 核心文件清单

| 文件 | 路径 |
|------|------|
| **MainActivity** | `ui/main/MainActivity.kt` |
| **OperitApp** | `ui/main/OperitApp.kt` |
| **PhoneLayout** | `ui/main/layout/PhoneLayout.kt` |
| **TabletLayout** | `ui/main/layout/TabletLayout.kt` |
| **AppContent** | `ui/main/components/AppContent.kt` |
| **DrawerContent** | `ui/main/components/DrawerContent.kt` |
| **OperitScreens** | `ui/main/screens/OperitScreens.kt` |
| **NavItem** | `ui/common/NavItem.kt` |
