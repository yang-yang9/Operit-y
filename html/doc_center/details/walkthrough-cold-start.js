registerWalkthroughSection('cold-start', '冷启动到主界面', '<div style="max-width:960px;margin:0 auto;padding:8px 0 48px;">\n\n<h2 class="section-title">Walkthrough: App 冷启动到主界面可交互</h2>\n\n<div class="callout"><b>场景</b><br/>用户点击 Operit 的 App 图标。从进程创建到看到聊天界面、可以发消息，中间经过了哪些代码。</div>\n\n<div class="callout"><b>阅读方式</b><br/>左边打开这篇文档，右边打开 Android Studio。每一步都标注了文件路径和行号，跟着跳转。<br/><b>预计时间：</b>30-40 分钟。</div>\n\n<h2 class="section-title">全链路总览</h2>\n\n<div class="mermaid">flowchart TD\n    A["进程创建"] --> B["Step 1: attachBaseContext()<br/>语言配置注入"]\n    B --> C["Step 2-7: Application.onCreate()<br/>34 步初始化"]\n    C --> C1["Step 3: 同步初始化<br/>全局单例 + Manager"]\n    C --> C2["Step 4: runBlocking<br/>前台服务判断"]\n    C --> C3["Step 5: runBlocking<br/>语言设置"]\n    C --> C4["Step 6: 异步初始化<br/>角色卡/Emoji/分词/数据库"]\n    C --> C5["Step 7: delay 800ms<br/>工具注册/磁盘预加载"]\n    C1 & C2 & C3 & C4 & C5 --> D["Step 8: MainActivity.onCreate()<br/>组件初始化 + 首帧渲染"]\n    D --> E["Step 9: performInitialChecks()<br/>协程中执行门控检查"]\n    E --> F{"Step 10: 门控渲染"}\n    F -->|协议未接受| F1["门1: AgreementScreen"]\n    F -->|需要迁移| F2["门2: MigrationScreen"]\n    F -->|需要权限| F3["门3: PermissionGuideScreen"]\n    F -->|全部通过| G["门4: 主界面<br/>+ 插件加载覆盖层"]\n    G --> H["Step 11: OperitApp<br/>导航初始化 + 异步任务"]</div>\n\n<h2 class="section-title">Step 1: 最早的入口 — 语言注入</h2>\n\n<p><code>📂 core/application/OperitApplication.kt L545-571</code></p>\n\n<pre><code>override fun attachBaseContext(base: Context) {\n    try {\n        configureOpenMpEnvironment()\n\n        // L549: 从 SharedPreferences 同步读取用户设置的语言\n        val code = LocaleUtils.getCurrentLanguage(base)\n        val locale = LocaleUtils.getLocaleForLanguageCode(code, base)\n\n        // L551: 用读到的语言创建新的 Configuration\n        val config = Configuration(base.resources.configuration)\n        // L554-561: 设置 Locale\n        if (Build.VERSION.SDK_INT &gt;= 24) {\n            config.setLocales(LocaleList(locale))\n        } else {\n            config.locale = locale\n        }\n\n        // L564: 用新 Configuration 创建 Context\n        val context = base.createConfigurationContext(config)\n        super.attachBaseContext(context)\n    } catch (e: Exception) {\n        super.attachBaseContext(base)\n    }\n}</code></pre>\n\n<p><b>为什么在这里做语言注入？</b><code>attachBaseContext</code> 是 Android 最早的入口点——比 <code>onCreate</code> 还早。如果等到 <code>onCreate</code> 再设置语言，那些在 <code>onCreate</code> 之前就通过 <code>getString(R.string.xxx)</code> 加载的资源字符串已经用了默认语言，改不回来了。</p>\n\n<div class="callout"><b>注意：</b>这里用的是 <code>SharedPreferences</code>（同步读取），不是 DataStore（异步读取）。因为 <code>attachBaseContext</code> 不能等协程。</div>\n\n<h2 class="section-title">Step 2: Application.onCreate — 34 步初始化概览</h2>\n\n<p><code>📂 core/application/OperitApplication.kt L116-369</code></p>\n\n<pre><code>override fun onCreate() {\n    super.onCreate()\n    val startTime = System.currentTimeMillis()\n    appStartupTimeMs = startTime\n    instance = this   // L120: 全局单例挂载\n    // ... 接下来 34 步初始化\n}</code></pre>\n\n<p>34 步初始化看起来很吓人，但它们归为 5 类任务。接下来按类型分段看。</p>\n\n<h2 class="section-title">Step 3: 同步初始化 — 全局单例和 Manager</h2>\n\n<p><code>📂 core/application/OperitApplication.kt L120-169</code></p>\n\n<table class="info-table">\n<thead><tr><th>行号</th><th>做什么</th><th>为什么必须同步</th></tr></thead>\n<tbody>\n<tr><td>L120</td><td><code>instance = this</code></td><td>全局单例，后续所有代码通过它获取 Application 引用</td></tr>\n<tr><td>L122</td><td><code>configureOpenMpEnvironment()</code></td><td>设置 CPU 亲和性，影响 NDK 推理性能</td></tr>\n<tr><td>L123</td><td><code>AppIconManager.ensureComponentState()</code></td><td>确保 App 图标组件状态正确</td></tr>\n<tr><td>L126</td><td><code>CrashRecoveryState.consumePendingCrashReportLaunch()</code></td><td>检测是否崩溃恢复启动</td></tr>\n<tr><td>L128</td><td><code>AppLogger.resetLogFile()</code></td><td>非崩溃恢复时重置日志文件</td></tr>\n<tr><td>L131</td><td><code>ensureWorkManagerInitialized()</code></td><td>WorkManager 初始化</td></tr>\n<tr><td>L144</td><td><code>ActivityLifecycleManager.initialize(this)</code></td><td>注册全局 Activity 生命周期回调</td></tr>\n<tr><td>L148</td><td><code>AIMessageManager.initialize(this)</code></td><td>消息管理器初始化</td></tr>\n<tr><td>L149</td><td><code>PluginRegistry.initializeBuiltins()</code></td><td>内置插件注册</td></tr>\n<tr><td>L169</td><td><code>Thread.setDefaultUncaughtExceptionHandler(...)</code></td><td>全局异常处理器</td></tr>\n<tr><td>L173</td><td>初始化 <code>Json</code> 实例</td><td>kotlinx.serialization 配置</td></tr>\n</tbody>\n</table>\n\n<p>这些步骤都是轻量级的（每个几毫秒），而且后续代码依赖它们的结果，所以必须同步执行。</p>\n\n<h2 class="section-title">Step 4: 阻塞读取 — 前台服务是否需要启动？</h2>\n\n<p><code>📂 core/application/OperitApplication.kt L471-493</code></p>\n\n<pre><code>private fun startGlobalAIForegroundServiceIfNeeded() {\n    try {\n        // ⚠️ 阻塞读取 1: 唤醒词是否开启\n        val alwaysListeningEnabled = runBlocking {\n            WakeWordPreferences(applicationContext)\n                .alwaysListeningEnabledFlow.first()\n        }\n        // ⚠️ 阻塞读取 2: 外部 HTTP API 是否开启\n        val externalHttpEnabled = runBlocking {\n            ExternalHttpApiPreferences.getInstance(applicationContext)\n                .enabledFlow.first()\n        }\n\n        // L479: 两个都关闭，且 Service 没在运行 → 不启动\n        if ((!alwaysListeningEnabled &amp;&amp; !externalHttpEnabled)\n            || AIForegroundService.isRunning.get()) {\n            return\n        }\n\n        // L482-489: 需要启动前台服务\n        val intent = Intent(this, AIForegroundService::class.java)\n        intent.putExtra(AIForegroundService.EXTRA_STATE, AIForegroundService.STATE_IDLE)\n        startForegroundService(intent)\n    } catch (e: Exception) {\n        AppLogger.e("OperitApp", "Failed to start foreground service", e)\n    }\n}</code></pre>\n\n<p><b>两次 <code>runBlocking</code></b>——在主线程同步等待 DataStore 读取完成。</p>\n\n<p><b>为什么不能异步？</b>如果异步读取唤醒词设置，等协程跑完的时候 Activity 可能已经创建了。此时用户说了唤醒词但 Service 还没启动，唤醒词无响应。必须在 <code>onCreate</code> 结束前就确定是否启动 Service。</p>\n\n<div class="callout"><b>会不会 ANR？</b><br/>DataStore 的 <code>first()</code> 读本地文件，通常几毫秒完成。ANR 阈值是 5 秒，远远够用。但如果 DataStore 文件损坏或磁盘 I/O 异常慢，理论上有 ANR 风险。</div>\n\n<h2 class="section-title">Step 5: 阻塞读取 — 语言设置</h2>\n\n<p><code>📂 core/application/OperitApplication.kt L496-543</code></p>\n\n<pre><code>private fun initializeAppLanguage() {\n    try {\n        // ⚠️ 阻塞读取 3: 语言设置\n        val languageCode = runBlocking {\n            try {\n                preferencesManager.appLanguage.first()\n            } catch (e: Exception) {\n                DEFAULT_LANGUAGE\n            }\n        }\n\n        val locale = LocaleUtils.getLocaleForLanguageCode(languageCode, this)\n        Locale.setDefault(locale)\n\n        // L521-537: 根据 API 版本应用语言设置\n        if (Build.VERSION.SDK_INT &gt;= 33) {\n            AppCompatDelegate.setApplicationLocales(LocaleListCompat.create(locale))\n        } else {\n            val config = Configuration(resources.configuration)\n            config.setLocales(LocaleList(locale))\n            resources.updateConfiguration(config, resources.displayMetrics)\n        }\n    } catch (e: Exception) {\n        AppLogger.e("OperitApp", "Failed to initialize language", e)\n    }\n}</code></pre>\n\n<div class="callout"><b>等等，Step 1 不是已经在 attachBaseContext 里设过语言了吗？</b><br/><code>attachBaseContext</code> 读的是 <code>SharedPreferences</code>（最基本的存储）。这里读的是 <code>DataStore</code>（更完整的用户偏好）。两者可能不一致——比如用户在上次运行时通过 Settings 改了语言（写入 DataStore），但 SharedPreferences 的缓存还没同步。这一步确保用 DataStore 的最新值覆盖。</div>\n\n<h2 class="section-title">Step 6: 异步初始化 — 不阻塞主线程的任务</h2>\n\n<p><code>📂 core/application/OperitApplication.kt L196-268</code></p>\n\n<p>这些任务通过 <code>scope.launch(Dispatchers.IO)</code> 在后台线程执行，不阻塞主线程：</p>\n\n<table class="info-table">\n<thead><tr><th>行号</th><th>任务</th><th>为什么可以异步</th></tr></thead>\n<tbody>\n<tr><td>L196-200</td><td><code>CharacterCardManager.initializeIfNeeded()</code></td><td>角色卡不影响首帧渲染</td></tr>\n<tr><td>L203-207</td><td><code>CustomEmojiRepository.initializeBuiltinEmojis()</code></td><td>Emoji 在聊天时才需要</td></tr>\n<tr><td>L252-256</td><td><code>TextSegmenter.initialize()</code></td><td>Jieba 分词预热，搜索时才需要</td></tr>\n<tr><td>L263-268</td><td><code>database.problemDao().getProblemCount()</code></td><td>Room 数据库预加载（触发建表）</td></tr>\n</tbody>\n</table>\n\n<p>中间穿插的同步步骤（L210 <code>AndroidShellExecutor</code>、L214-239 <code>ShowerEnvironment</code>、L244 <code>PDFBoxResourceLoader</code>、L248 <code>LanguageFactory</code>）都是轻量级的配置设置。</p>\n\n<h2 class="section-title">Step 7: 延迟 800ms — 等首帧渲染完再干重活</h2>\n\n<p><code>📂 core/application/OperitApplication.kt L317-333</code></p>\n\n<pre><code>applicationScope.launch(Dispatchers.Default) {\n    delay(800)  // L319: 给 UI 线程留出首帧渲染时间\n\n    // 1. 图片池从磁盘预加载\n    withContext(Dispatchers.IO) { ImagePoolManager.preloadFromDisk() }\n\n    // 2. 媒体池从磁盘预加载\n    withContext(Dispatchers.IO) { MediaPoolManager.preloadFromDisk() }\n\n    // 3. 注册默认工具\n    val toolHandler = AIToolHandler.getInstance(this@OperitApplication)\n    toolHandler.registerDefaultTools()\n}</code></pre>\n\n<p><b>为什么延迟 800ms？</b><code>Application.onCreate</code> 结束后，系统要执行 <code>Activity.onCreate → setContent → Compose 首帧布局和渲染</code>。这个过程大约 500-800ms。如果在此期间做磁盘 I/O（预加载图片/媒体池）和 CPU 密集操作（工具注册），会抢占主线程 CPU 时间，导致首帧掉帧。</p>\n\n<div class="callout"><b>验证方式：</b>把 <code>delay(800)</code> 改成 <code>delay(0)</code>，重新编译运行，看启动动画是否变卡。改完记得恢复。</div>\n\n<h2 class="section-title">Step 8: MainActivity.onCreate — 组件初始化 + 首帧渲染</h2>\n\n<p><code>📂 ui/main/MainActivity.kt L256-302</code></p>\n\n<pre><code>override fun onCreate(savedInstanceState: Bundle?) {\n    super.onCreate(savedInstanceState)\n\n    // L261: 窗口背景设为黑色（防止系统主题色渗透）\n    window.decorView.setBackgroundColor(Color.Black.toArgb())\n\n    // L265: 处理 Intent（分享文件/链接/OAuth 回调/快捷方式）\n    handleIntent(intent)\n\n    // L270: 初始化核心组件\n    initializeComponents()\n\n    // L271-273: ANR 监控 + 偏好监听 + 显示设置（高刷新率等）\n    anrMonitor.start()\n    setupPreferencesListener()\n    configureDisplaySettings()\n\n    // L286: ⭐ 首帧渲染\n    setAppContent()\n\n    // L289: 延迟 3 秒检查更新\n    setupUpdateManager()\n\n    // L291-299: 门控检查\n    if (savedInstanceState == null) {\n        performInitialChecks()   // 冷启动 → 执行门控\n    } else {\n        initialChecksDone = true // 配置变更重建 → 跳过门控\n    }\n}</code></pre>\n\n<p><b>关键设计：</b>L286 的 <code>setAppContent()</code> 在 <code>performInitialChecks()</code> 之前调用。此时 <code>initialChecksDone = false</code>，所以渲染的是空白占位。但系统已经知道"Activity 有内容了"——从黑屏变成了空白屏。虽然用户看不出区别，但系统层面这很重要：系统会停止显示 App 启动的白屏闪烁。</p>\n\n<p><code>initializeComponents</code>（L601-623）实例化 7 个组件：<code>toolHandler</code>、<code>mcpRepository</code>、<code>anrMonitor</code>、<code>preferencesManager</code>、<code>showPreferencesGuide</code>、<code>agreementPreferences</code>、<code>migrationManager</code>。</p>\n\n<h2 class="section-title">Step 9: 门控检查 — 协程中异步执行</h2>\n\n<p><code>📂 ui/main/MainActivity.kt L428-461</code></p>\n\n<pre><code>private fun performInitialChecks() {\n    lifecycleScope.launch {\n        // 1. 通知权限（Android 13+ 需要运行时请求 POST_NOTIFICATIONS）\n        checkNotificationPermission()\n\n        // 2. 权限级别检查\n        checkPermissionLevelSet()\n        // → 如果 getPreferredPermissionLevel() == null\n        //   → showPermissionGuide = true\n\n        // 3. 协议 + 迁移检查\n        if (!showPermissionGuide &amp;&amp; agreementPreferences.isAgreementAccepted()) {\n            try {\n                if (migrationManager.needsMigration()) {\n                    showMigrationScreen = true\n                } else {\n                    startPluginLoading()\n                }\n            } catch (e: Exception) {\n                startPluginLoading()  // 迁移检查失败也继续\n            }\n        }\n\n        // 4. 标记完成，刷新渲染\n        initialChecksDone = true\n        setAppContent()\n    }\n}</code></pre>\n\n<p><b>执行顺序：</b>先查通知权限 → 再查有没有设过权限级别 → 再查协议和迁移。三个检查是串行的，前一个的结果影响后一个。</p>\n\n<p><code>initialChecksDone = true</code> 之后调用 <code>setAppContent()</code>——这是第二次调用 setAppContent。这次会根据检查结果渲染正确的页面。</p>\n\n<h2 class="section-title">Step 10: 门控渲染 — if/else 链 = 优先级队列</h2>\n\n<p><code>📂 ui/main/MainActivity.kt L724-825</code></p>\n\n<pre><code>private fun setAppContent() {\n    setContent {\n        OperitTheme {\n            Box {\n                // 门 0: 还没检查完 → 空白占位\n                if (!initialChecksDone) {\n                    // 什么都不渲染\n                }\n                // 门 1: 用户协议未接受\n                else if (!agreementPreferences.isAgreementAccepted()) {\n                    AgreementScreen(\n                        onAgreementAccepted = {\n                            agreementPreferences.setAgreementAccepted(true)\n                            lifecycleScope.launch {\n                                delay(300)\n                                checkPermissionLevelSet()\n                                setAppContent()  // 递归刷新\n                            }\n                        }\n                    )\n                }\n                // 门 2: 需要数据迁移\n                else if (showMigrationScreen) {\n                    MigrationScreen(\n                        onComplete = {\n                            showMigrationScreen = false\n                            startPluginLoading()\n                            setAppContent()  // 递归刷新\n                        }\n                    )\n                }\n                // 门 3: 需要权限引导\n                else if (showPermissionGuide) {\n                    PermissionGuideScreen(\n                        onComplete = {\n                            showPermissionGuide = false\n                            setAppContent()  // 递归刷新\n                        }\n                    )\n                }\n                // 门 4: 所有门通过 → 主界面\n                else {\n                    val initialNavItem = when {\n                        showPreferencesGuide -&gt; NavItem.UserPreferencesGuide\n                        shortcutNavItem != null -&gt; shortcutNavItem!!\n                        else -&gt; NavItem.AiChat\n                    }\n                    CompositionLocalProvider(LocalPluginLoadingState provides pluginLoadingState) {\n                        OperitApp(initialNavItem = initialNavItem, ...)\n                    }\n                }\n\n                // 插件加载覆盖层（zIndex=10，浮在所有内容之上）\n                PluginLoadingScreenWithState(\n                    pluginLoadingState = pluginLoadingState,\n                    modifier = Modifier.zIndex(10f)\n                )\n            }\n        }\n    }\n}</code></pre>\n\n<div class="callout"><b>关键设计模式：if/else 链 = 优先级队列</b><br/>协议 &gt; 迁移 &gt; 权限 &gt; 主界面。每道门通过后调用 <code>setAppContent()</code> 递归刷新，进入下一道门。<br/><br/><b>门的顺序不能调换：</b><br/>• 协议必须最先（法律要求，未接受不能使用任何功能）<br/>• 迁移在权限之前（老数据必须迁移完才能正常使用）<br/>• 权限在主界面之前（AI 工具需要知道权限级别）</div>\n\n<p><b>插件加载覆盖层（L804）：</b><code>PluginLoadingScreenWithState</code> 用 <code>zIndex(10f)</code> 浮在所有内容之上。它是<b>非阻塞的</b>——主界面（<code>OperitApp</code>）已经在后台渲染和初始化了，插件加载完毕后覆盖层消失，用户立即可交互。</p>\n\n<h2 class="section-title">Step 11: OperitApp — Compose 世界的入口</h2>\n\n<p><code>📂 ui/main/OperitApp.kt L55-62</code></p>\n\n<pre><code>@Composable\nfun OperitApp(\n    initialNavItem: NavItem = NavItem.AiChat,\n    toolHandler: AIToolHandler? = null,\n    shortcutNavRequest: NavItem? = null,\n    shortcutNavRequestId: Long = 0L,\n    onShortcutNavHandled: (Long) -> Unit = {}\n)</code></pre>\n\n<h3>11.1 导航初始化（L71-75）</h3>\n\n<pre><code>var selectedItem by remember { mutableStateOf(initialNavItem) }\nvar currentScreen by remember {\n    mutableStateOf(OperitRouter.getScreenForNavItem(initialNavItem))\n}\nval backStack = remember { mutableStateListOf&lt;Screen&gt;() }</code></pre>\n\n<div class="callout"><b>为什么自定义导航栈而不用 Jetpack Navigation？</b><br/>Operit 有 30+ 个页面，Jetpack Navigation 的 XML 路由图对于这种规模容易变得难以维护。自定义方案用 sealed class <code>Screen</code> 枚举所有页面，类型系统在编译期就能捕获错误的路由跳转。<code>SnapshotStateList&lt;Screen&gt;</code> 手动管理回退栈，<code>navigateTo</code> 压栈，<code>goBack</code> 弹栈，逻辑完全透明可控。</div>\n\n<h3>11.2 异步任务启动（LaunchedEffect 块）</h3>\n\n<pre><code>// L240: 每 10 秒轮询网络状态\nLaunchedEffect(Unit) {\n    while (true) {\n        isNetworkAvailable = checkNetworkAvailability()\n        delay(10000)\n    }\n}\n\n// L249: 网络可用时拉取远程公告\nLaunchedEffect(isNetworkAvailable) {\n    if (isNetworkAvailable &amp;&amp; !isShowingAnnouncement) {\n        fetchRemoteAnnouncement()\n    }\n}\n\n// L266: 同步 MCP 插件安装状态\nLaunchedEffect(Unit) {\n    mcpRepository.syncInstalledStatus()\n}</code></pre>\n\n<h3>11.3 布局选择（L191, L281）</h3>\n\n<pre><code>// L191: 平板/手机判断\nval useTabletLayout = screenWidthDp &gt;= 600\n\n// L281: 选择布局\nif (useTabletLayout) {\n    TabletLayout(...)   // 永久侧边栏\n} else {\n    PhoneLayout(...)    // 手势抽屉\n}</code></pre>\n\n<p><b>600dp</b> 是 Material Design 3 的分界点。平板用永久可见的侧边导航栏，手机用手势滑出的抽屉式导航。</p>\n\n<p><b>至此，用户看到了聊天界面，可以开始交互。</b></p>\n\n<h2 class="section-title">完整调用链回顾</h2>\n\n<pre><code>用户点击 App 图标\n│\n├─ 进程创建\n│\n├─ Step 1:  attachBaseContext()                    [L545] SharedPreferences 读语言\n│\n├─ Step 2-7: Application.onCreate()               [L116] 34 步初始化\n│   ├─ Step 3:  同步初始化（L120-169）             单例 + Manager + 异常处理\n│   ├─ Step 4:  startGlobalAIForegroundServiceIfNeeded() [L471]\n│   │           ⚠️ 2 次 runBlocking 读 DataStore\n│   ├─ Step 5:  initializeAppLanguage()            [L496]\n│   │           ⚠️ 1 次 runBlocking 读 DataStore\n│   ├─ Step 6:  异步初始化（L196-268）             角色卡/Emoji/分词/数据库\n│   └─ Step 7:  delay(800ms)（L317）               工具注册/磁盘预加载\n│\n├─ Step 8:  MainActivity.onCreate()                [L256]\n│   ├─ initializeComponents()                      [L601] 7 个组件实例化\n│   ├─ setAppContent() ⭐ 首帧渲染                  [L724] 空白占位\n│   └─ performInitialChecks()                      [L428] 协程启动\n│\n├─ Step 9:  门控检查（协程中）\n│   ├─ 通知权限 → 权限级别 → 协议 → 迁移\n│   └─ initialChecksDone = true → setAppContent()\n│\n├─ Step 10: setAppContent() 门控渲染               [L724]\n│   ├─ 协议未接受 → AgreementScreen\n│   ├─ 需要迁移   → MigrationScreen\n│   ├─ 需要权限   → PermissionGuideScreen\n│   └─ 全部通过   → OperitApp + PluginLoadingScreen（zIndex=10）\n│\n└─ Step 11: OperitApp                              [L55]\n    ├─ 导航初始化: backStack + Screen                [L71]\n    ├─ 异步任务: 网络轮询 / 公告 / MCP 同步          [L240-266]\n    ├─ 布局选择: &gt;= 600dp → TabletLayout            [L191]\n    └─ ⭐ 用户看到 AiChat 页面，可交互\n\n涉及文件（按调用顺序）:\n1. core/application/OperitApplication.kt\n2. ui/main/MainActivity.kt\n3. ui/main/OperitApp.kt</code></pre>\n\n<h2 class="section-title">动手练习</h2>\n\n<h3>练习 1: 启动计时</h3>\n\n<p>在 <code>OperitApplication.onCreate</code> 的 L118 已经有启动时间戳 <code>startTime</code>。在以下关键点加日志，记录耗时：</p>\n<ol>\n<li>L162 <code>startGlobalAIForegroundServiceIfNeeded()</code> 前后</li>\n<li>L183 <code>initializeAppLanguage()</code> 前后</li>\n<li>L286 <code>setAppContent()</code> 前后</li>\n</ol>\n\n<pre><code>adb logcat | grep "启动计时"</code></pre>\n\n<p><b>练习目标：</b>找到 3 次 <code>runBlocking</code> 各自的耗时。</p>\n\n<h3>练习 2: 跳过门控</h3>\n\n<p>在 <code>performInitialChecks()</code> 第一行加上：</p>\n<pre><code>initialChecksDone = true; setAppContent(); return@launch</code></pre>\n\n<p>重新安装（全新安装），观察：协议页消失了，但 AI 可能因为权限未设置而无法正常使用。</p>\n\n<p><b>练习目标：</b>理解门控不是"烦人的欢迎页"，而是保障数据完整性和法律合规的必要步骤。</p>\n\n<h3>练习 3: 观察插件加载</h3>\n\n<p>在 <code>startPluginLoading()</code> 里找到 <code>pluginLoadingState.initializeMCPServer</code>。在每个插件启动前后加日志。观察每个 MCP 插件的启动耗时——这就是为什么需要 30 秒超时。</p>\n\n<h2 class="section-title">关联文档</h2>\n\n<table class="info-table">\n<thead><tr><th>文档</th><th>关系</th></tr></thead>\n<tbody>\n<tr><td><code>41_Tutorial.启动链路精讲.md</code></td><td>概念层 — 理解 WHY（5 个关键设计决策）</td></tr>\n<tr><td><code>37_Runtime.冷启动全链路.md</code></td><td>参考手册 — 34 步完整行号索引</td></tr>\n<tr><td><code>chat-message-flow.md</code></td><td>下一篇导读 — 核心业务链路</td></tr>\n<tr><td><code>tool-execution.md</code></td><td>再下一篇 — 工具系统</td></tr>\n</tbody>\n</table>\n\n</div>');
